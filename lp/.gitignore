dist/
.astro/
node_modules/

# No .env is committed, only the example. Even the settings here are public
# once built — they end up in the page — but a file called .env is where a
# secret eventually gets written, and that is the file nobody should have to
# think twice about.
.env*
!.env.example
