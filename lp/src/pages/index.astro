---
// English at the root of /lp, because hledger's own documentation is in it and
// that is where somebody arriving from hledger.org comes from.
import Page from "../Page.astro"
import { en } from "../words"
---

<Page words={en} />
