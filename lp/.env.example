# Copy to .env to override. This is not a secret: it ends up in the page as
# built. Without any .env at all, `bun run dev` links to the app on this
# machine and `bun run build` links to the published one.

# Where the app itself is. The only setting there is.
PUBLIC_APP=https://std.choai.dev
